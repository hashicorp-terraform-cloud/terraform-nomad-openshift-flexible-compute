---
- name: Determine package-managed remove support
  ansible.builtin.set_fact:
    nomad_client_remove_use_package_manager: >-
      {{
        (ansible_facts["os_family"] in ["RedHat", "Debian"])
        and (ansible_facts.get("distribution", "") != "Amazon")
      }}

- name: Require Nomad ACL token for drain/purge operations when ACL is enabled
  ansible.builtin.assert:
    that:
      - (nomad_client_remove_nomad_token | trim | length) > 0
    fail_msg: >-
      Nomad ACL is enabled for remove workflow, but no nomad_token was provided.
      Provide nomad_token (management or sufficient client token) via extra vars.
  when:
    - nomad_client_remove_acl_enabled | bool
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)

- name: Build Nomad CLI environment for remove workflow
  ansible.builtin.set_fact:
    nomad_client_remove_nomad_env: >-
      {{
        {}
        | combine(
            {
              'NOMAD_ADDR': (
                (nomad_client_remove_tls_enabled | bool)
                | ternary('https://127.0.0.1:4646', 'http://127.0.0.1:4646')
              )
            }
          )
        | combine(
            (nomad_client_remove_nomad_token | trim | length > 0)
            | ternary({'NOMAD_TOKEN': nomad_client_remove_nomad_token | trim}, {})
          )
        | combine(
            (nomad_client_remove_tls_enabled | bool)
            | ternary({'NOMAD_CACERT': nomad_client_remove_tls_ca_file}, {})
          )
        | combine(
            (nomad_client_remove_tls_enabled | bool)
            | ternary({'NOMAD_CLIENT_CERT': nomad_client_remove_tls_cert_file}, {})
          )
        | combine(
            (nomad_client_remove_tls_enabled | bool)
            | ternary({'NOMAD_CLIENT_KEY': nomad_client_remove_tls_key_file}, {})
          )
      }}
  when: (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)

- name: Resolve Nomad API address for node purge
  ansible.builtin.set_fact:
    nomad_client_remove_nomad_addr_purge: >-
      {{
        (nomad_client_remove_nomad_addr | trim)
        if (nomad_client_remove_nomad_addr | trim | length > 0)
        else (
          (((nomad_client_remove_tls_enabled | bool) | ternary('https://', 'http://')) ~ (nomad_client_remove_nomad_server_address | trim) ~ ':4646')
          if (nomad_client_remove_nomad_server_address | trim | length > 0)
          else ''
        )
      }}
  when: nomad_client_remove_purge_node | bool

- name: Build Nomad CLI environment for node purge
  ansible.builtin.set_fact:
    nomad_client_remove_nomad_env_purge: >-
      {{
        {}
        | combine(
            (nomad_client_remove_nomad_addr_purge | trim | length > 0)
            | ternary({'NOMAD_ADDR': nomad_client_remove_nomad_addr_purge | trim}, {})
          )
        | combine(
            (nomad_client_remove_nomad_token | trim | length > 0)
            | ternary({'NOMAD_TOKEN': nomad_client_remove_nomad_token | trim}, {})
          )
        | combine(
            (nomad_client_remove_tls_enabled | bool)
            | ternary({'NOMAD_CACERT': nomad_client_remove_tls_ca_file}, {})
          )
        | combine(
            (nomad_client_remove_tls_enabled | bool)
            | ternary({'NOMAD_CLIENT_CERT': nomad_client_remove_tls_cert_file}, {})
          )
        | combine(
            (nomad_client_remove_tls_enabled | bool)
            | ternary({'NOMAD_CLIENT_KEY': nomad_client_remove_tls_key_file}, {})
          )
      }}
  when: nomad_client_remove_purge_node | bool

- name: Discover Nomad CLI path (POSIX)
  ansible.builtin.shell: |
    set -euo pipefail
    if command -v nomad >/dev/null 2>&1; then
      command -v nomad
    elif [[ -x "{{ nomad_client_remove_install_dir }}/nomad" ]]; then
      printf '%s\n' "{{ nomad_client_remove_install_dir }}/nomad"
    elif [[ -x /usr/bin/nomad ]]; then
      printf '%s\n' /usr/bin/nomad
    fi
  args:
    executable: /bin/bash
  register: nomad_client_remove_cli_path_posix
  changed_when: false
  failed_when: false
  when:
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)
    - ansible_facts["system"] in ["Linux", "Darwin"]

- name: Note missing Nomad CLI before remove workflow (POSIX)
  ansible.builtin.debug:
    msg: >-
      Nomad CLI is already absent on this host; skipping pre-uninstall self drain and purge steps.
  when:
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) == 0

- name: Discover Nomad CLI path (Windows)
  ansible.windows.win_shell: |
    $cmd = Get-Command nomad.exe -ErrorAction SilentlyContinue
    if ($null -ne $cmd) {
      Write-Output $cmd.Source
    }
    elseif (Test-Path "{{ nomad_client_remove_install_dir }}\nomad.exe") {
      Write-Output "{{ nomad_client_remove_install_dir }}\nomad.exe"
    }
  register: nomad_client_remove_cli_path_windows
  changed_when: false
  failed_when: false
  when:
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)
    - ansible_facts["os_family"] == "Windows"

- name: Note missing Nomad CLI before remove workflow (Windows)
  ansible.builtin.debug:
    msg: >-
      Nomad CLI is already absent on this host; skipping pre-uninstall self drain and purge steps.
  when:
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)
    - ansible_facts["os_family"] == "Windows"
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) == 0

- name: Disable node eligibility before uninstall (POSIX)
  ansible.builtin.command:
    argv:
      - "{{ nomad_client_remove_cli_path_posix.stdout | trim }}"
      - node
      - eligibility
      - -disable
      - -self
  environment: "{{ nomad_client_remove_nomad_env }}"
  register: nomad_client_remove_disable_eligibility_posix
  changed_when: nomad_client_remove_disable_eligibility_posix.rc == 0
  failed_when: false
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Assert node eligibility disable succeeded (POSIX)
  ansible.builtin.assert:
    that:
      - nomad_client_remove_disable_eligibility_posix.rc == 0
    fail_msg: >-
      Failed to disable Nomad node eligibility before uninstall.
      rc={{ nomad_client_remove_disable_eligibility_posix.rc | default('n/a') }}
      stdout={{ nomad_client_remove_disable_eligibility_posix.stdout | default('') }}
      stderr={{ nomad_client_remove_disable_eligibility_posix.stderr | default('') }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Drain node before uninstall (POSIX)
  ansible.builtin.command:
    argv: >-
      {{
        [(nomad_client_remove_cli_path_posix.stdout | trim), 'node', 'drain', '-enable', '-yes']
        + (
            (nomad_client_remove_drain_force | bool)
            | ternary(['-force'], ['-deadline=' ~ nomad_client_remove_drain_deadline])
          )
        + ['-self']
      }}
  environment: "{{ nomad_client_remove_nomad_env }}"
  register: nomad_client_remove_drain_posix
  changed_when: nomad_client_remove_drain_posix.rc == 0
  failed_when: false
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Assert node drain succeeded (POSIX)
  ansible.builtin.assert:
    that:
      - nomad_client_remove_drain_posix.rc == 0
    fail_msg: >-
      Failed to drain Nomad node before uninstall.
      rc={{ nomad_client_remove_drain_posix.rc | default('n/a') }}
      stdout={{ nomad_client_remove_drain_posix.stdout | default('') }}
      stderr={{ nomad_client_remove_drain_posix.stderr | default('') }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Disable node eligibility before uninstall (Windows)
  ansible.windows.win_shell: |
    $env:NOMAD_ADDR = "{{ nomad_client_remove_nomad_env.NOMAD_ADDR | default('') }}"
    $env:NOMAD_TOKEN = "{{ nomad_client_remove_nomad_env.NOMAD_TOKEN | default('') }}"
    $env:NOMAD_CACERT = "{{ nomad_client_remove_nomad_env.NOMAD_CACERT | default('') }}"
    $env:NOMAD_CLIENT_CERT = "{{ nomad_client_remove_nomad_env.NOMAD_CLIENT_CERT | default('') }}"
    $env:NOMAD_CLIENT_KEY = "{{ nomad_client_remove_nomad_env.NOMAD_CLIENT_KEY | default('') }}"
    & "{{ nomad_client_remove_cli_path_windows.stdout | trim }}" node eligibility -disable -self
  register: nomad_client_remove_disable_eligibility_windows
  changed_when: nomad_client_remove_disable_eligibility_windows.rc == 0
  failed_when: false
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) > 0

- name: Assert node eligibility disable succeeded (Windows)
  ansible.builtin.assert:
    that:
      - nomad_client_remove_disable_eligibility_windows.rc == 0
    fail_msg: >-
      Failed to disable Nomad node eligibility before uninstall on Windows.
      rc={{ nomad_client_remove_disable_eligibility_windows.rc | default('n/a') }}
      stdout={{ nomad_client_remove_disable_eligibility_windows.stdout | default('') }}
      stderr={{ nomad_client_remove_disable_eligibility_windows.stderr | default('') }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) > 0

- name: Drain node before uninstall (Windows)
  ansible.windows.win_shell: |
    $env:NOMAD_ADDR = "{{ nomad_client_remove_nomad_env.NOMAD_ADDR | default('') }}"
    $env:NOMAD_TOKEN = "{{ nomad_client_remove_nomad_env.NOMAD_TOKEN | default('') }}"
    $env:NOMAD_CACERT = "{{ nomad_client_remove_nomad_env.NOMAD_CACERT | default('') }}"
    $env:NOMAD_CLIENT_CERT = "{{ nomad_client_remove_nomad_env.NOMAD_CLIENT_CERT | default('') }}"
    $env:NOMAD_CLIENT_KEY = "{{ nomad_client_remove_nomad_env.NOMAD_CLIENT_KEY | default('') }}"
    $drainArgs = @(
      'node', 'drain',
      '-enable',
      '-yes',
      {% if nomad_client_remove_drain_force | bool %}'-force',{% else %}'-deadline={{ nomad_client_remove_drain_deadline }}',{% endif %}
      '-self'
    )
    & "{{ nomad_client_remove_cli_path_windows.stdout | trim }}" @drainArgs
  register: nomad_client_remove_drain_windows
  changed_when: nomad_client_remove_drain_windows.rc == 0
  failed_when: false
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) > 0

- name: Assert node drain succeeded (Windows)
  ansible.builtin.assert:
    that:
      - nomad_client_remove_drain_windows.rc == 0
    fail_msg: >-
      Failed to drain Nomad node before uninstall on Windows.
      rc={{ nomad_client_remove_drain_windows.rc | default('n/a') }}
      stdout={{ nomad_client_remove_drain_windows.stdout | default('') }}
      stderr={{ nomad_client_remove_drain_windows.stderr | default('') }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) > 0

- name: Build Nomad API headers for allocation checks
  ansible.builtin.set_fact:
    nomad_client_remove_nomad_api_headers: >-
      {{
        {}
        | combine(
            (nomad_client_remove_nomad_token | trim | length > 0)
            | ternary({'X-Nomad-Token': nomad_client_remove_nomad_token | trim}, {})
          )
      }}
  when: nomad_client_remove_drain_before_uninstall | bool

- name: Lookup self node ID (POSIX)
  ansible.builtin.command:
    argv:
      - "{{ nomad_client_remove_cli_path_posix.stdout | trim }}"
      - node
      - status
      - -self
      - -json
  environment: "{{ nomad_client_remove_nomad_env }}"
  register: nomad_client_remove_self_node_status_posix
  changed_when: false
  failed_when: false
  when:
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Query self node allocations (POSIX)
  ansible.builtin.shell: |
    set -euo pipefail
    curl_args=(--silent --show-error --fail)
    {% if nomad_client_remove_tls_enabled | bool %}
    curl_args+=(--cacert "{{ nomad_client_remove_tls_ca_file }}")
    curl_args+=(--cert "{{ nomad_client_remove_tls_cert_file }}")
    curl_args+=(--key "{{ nomad_client_remove_tls_key_file }}")
    {% endif %}
    {% if (nomad_client_remove_nomad_token | trim | length) > 0 %}
    curl_args+=(--header "X-Nomad-Token: {{ nomad_client_remove_nomad_token | trim }}")
    {% endif %}
    allocations_url="{{
      (nomad_client_remove_nomad_env.NOMAD_ADDR | default('https://127.0.0.1:4646'))
      ~ '/v1/node/'
      ~ ((nomad_client_remove_self_node_status_posix.stdout | from_json).ID)
      ~ '/allocations'
    }}"
    curl "${curl_args[@]}" "${allocations_url}"
  args:
    executable: /bin/bash
  register: nomad_client_remove_self_allocations_posix
  changed_when: false
  failed_when: false
  until: >-
    (
      ((nomad_client_remove_self_allocations_posix.stdout | default('[]')) | from_json)
      | rejectattr('ClientStatus', 'in', ['complete', 'failed', 'lost'])
      | list
      | length
    ) == 0
  retries: "{{ nomad_client_remove_allocations_wait_retries }}"
  delay: "{{ nomad_client_remove_allocations_wait_delay_seconds }}"
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - nomad_client_remove_self_node_status_posix.get('rc', 1) == 0
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Assert self node allocations query succeeded (POSIX)
  ansible.builtin.assert:
    that:
      - nomad_client_remove_self_allocations_posix.rc == 0
    fail_msg: >-
      Failed to query self node allocations after drain.
      rc={{ nomad_client_remove_self_allocations_posix.rc | default('n/a') }}
      stdout={{ nomad_client_remove_self_allocations_posix.stdout | default('') }}
      stderr={{ nomad_client_remove_self_allocations_posix.stderr | default('') }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - nomad_client_remove_self_node_status_posix.get('rc', 1) == 0
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Normalize self node allocations response (POSIX)
  ansible.builtin.set_fact:
    nomad_client_remove_self_allocations_posix_list: >-
      {{
        (nomad_client_remove_self_allocations_posix.stdout | default('[]')) | from_json
      }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - nomad_client_remove_self_node_status_posix.get('rc', 1) == 0
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0
    - nomad_client_remove_self_allocations_posix.rc == 0

- name: Fail when active allocations remain on self node (POSIX)
  ansible.builtin.assert:
    that:
      - >-
        (
          nomad_client_remove_self_allocations_posix_list
          | rejectattr('ClientStatus', 'in', ['complete', 'failed', 'lost'])
          | list
          | length
        ) == 0
    fail_msg: >-
      Active allocations remain on self node after drain.
      Remaining allocation IDs: {{
        nomad_client_remove_self_allocations_posix_list
        | rejectattr('ClientStatus', 'in', ['complete', 'failed', 'lost'])
        | map(attribute='ID')
        | list
      }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - nomad_client_remove_self_node_status_posix.get('rc', 1) == 0
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0
    - nomad_client_remove_self_allocations_posix.rc == 0

- name: Lookup self node ID (Windows)
  ansible.windows.win_shell: |
    $env:NOMAD_ADDR = "{{ nomad_client_remove_nomad_env.NOMAD_ADDR | default('') }}"
    $env:NOMAD_TOKEN = "{{ nomad_client_remove_nomad_env.NOMAD_TOKEN | default('') }}"
    $env:NOMAD_CACERT = "{{ nomad_client_remove_nomad_env.NOMAD_CACERT | default('') }}"
    $env:NOMAD_CLIENT_CERT = "{{ nomad_client_remove_nomad_env.NOMAD_CLIENT_CERT | default('') }}"
    $env:NOMAD_CLIENT_KEY = "{{ nomad_client_remove_nomad_env.NOMAD_CLIENT_KEY | default('') }}"
    $selfNode = & "{{ nomad_client_remove_cli_path_windows.stdout | trim }}" node status -self -json
    ($selfNode | ConvertFrom-Json).ID
  register: nomad_client_remove_self_node_id_windows
  changed_when: false
  failed_when: false
  when:
    - (nomad_client_remove_drain_before_uninstall | bool) or (nomad_client_remove_purge_node | bool)
    - ansible_facts["os_family"] == "Windows"
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) > 0

- name: Query self node allocations (Windows)
  ansible.windows.win_uri:
    url: >-
      {{
        ((nomad_client_remove_nomad_env.NOMAD_ADDR | default(''))
          if (nomad_client_remove_nomad_env.NOMAD_ADDR | default('') | trim | length) > 0
          else ((nomad_client_remove_tls_enabled | bool) | ternary('https://127.0.0.1:4646', 'http://127.0.0.1:4646')))
        ~ '/v1/node/'
        ~ (nomad_client_remove_self_node_id_windows.stdout | trim)
        ~ '/allocations'
      }}
    method: GET
    headers: "{{ nomad_client_remove_nomad_api_headers }}"
    validate_certs: false
  register: nomad_client_remove_self_allocations_windows
  changed_when: false
  failed_when: false
  until: >-
    (
      (nomad_client_remove_self_allocations_windows.json | default([]))
      | rejectattr('ClientStatus', 'in', ['complete', 'failed', 'lost'])
      | list
      | length
    ) == 0
  retries: "{{ nomad_client_remove_allocations_wait_retries }}"
  delay: "{{ nomad_client_remove_allocations_wait_delay_seconds }}"
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_self_node_id_windows.get('rc', 1) == 0
    - (nomad_client_remove_self_node_id_windows.stdout | default('') | trim | length) > 0
    - not (nomad_client_remove_tls_enabled | bool)

- name: Assert self node allocations query succeeded (Windows)
  ansible.builtin.assert:
    that:
      - nomad_client_remove_self_allocations_windows.get('status_code', 0) == 200
      - nomad_client_remove_self_allocations_windows.get('json', none) is not none
    fail_msg: >-
      Failed to query self node allocations after drain on Windows.
      status={{ nomad_client_remove_self_allocations_windows.get('status_code', 'n/a') }}
      msg={{ nomad_client_remove_self_allocations_windows.get('msg', '') }}
      content={{ nomad_client_remove_self_allocations_windows.get('content', '') }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_self_node_id_windows.get('rc', 1) == 0
    - (nomad_client_remove_self_node_id_windows.stdout | default('') | trim | length) > 0
    - not (nomad_client_remove_tls_enabled | bool)

- name: Normalize self node allocations response (Windows)
  ansible.builtin.set_fact:
    nomad_client_remove_self_allocations_windows_list: >-
      {{
        nomad_client_remove_self_allocations_windows.get('json', [])
      }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_self_node_id_windows.get('rc', 1) == 0
    - (nomad_client_remove_self_node_id_windows.stdout | default('') | trim | length) > 0
    - not (nomad_client_remove_tls_enabled | bool)

- name: Fail when active allocations remain on self node (Windows)
  ansible.builtin.assert:
    that:
      - >-
        (
          nomad_client_remove_self_allocations_windows_list
          | rejectattr('ClientStatus', 'in', ['complete', 'failed', 'lost'])
          | list
          | length
        ) == 0
    fail_msg: >-
      Active allocations remain on self node after drain.
      Remaining allocation IDs: {{
        nomad_client_remove_self_allocations_windows_list
        | rejectattr('ClientStatus', 'in', ['complete', 'failed', 'lost'])
        | map(attribute='ID')
        | list
      }}
  when:
    - nomad_client_remove_drain_before_uninstall | bool
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_self_node_id_windows.get('rc', 1) == 0
    - (nomad_client_remove_self_node_id_windows.stdout | default('') | trim | length) > 0
    - not (nomad_client_remove_tls_enabled | bool)

- name: Stop Nomad service (Linux)
  ansible.builtin.service:
    name: "{{ nomad_client_remove_service_name }}"
    state: stopped
    enabled: false
  register: nomad_client_remove_stop_service_linux
  failed_when: false
  when: ansible_facts["system"] == "Linux"

- name: Stop Nomad service (macOS)
  ansible.builtin.shell: |
    if launchctl print system/{{ nomad_client_remove_launchd_label }} >/dev/null 2>&1; then
      launchctl bootout system/{{ nomad_client_remove_launchd_label }}
    fi
  args:
    executable: /bin/bash
  changed_when: true
  failed_when: false
  when: ansible_facts["system"] == "Darwin"

- name: Stop Nomad service (Windows)
  ansible.windows.win_service:
    name: "{{ nomad_client_remove_service_name }}"
    state: stopped
    start_mode: disabled
  register: nomad_client_remove_stop_service_windows
  failed_when: false
  when: ansible_facts["os_family"] == "Windows"

- name: Purge node from Nomad server list (POSIX)
  ansible.builtin.command:
    argv:
      - "{{ nomad_client_remove_cli_path_posix.stdout | trim }}"
      - node
      - purge
      - -yes
      - "{{ (nomad_client_remove_self_node_status_posix.stdout | from_json).ID }}"
  environment: "{{ nomad_client_remove_nomad_env_purge }}"
  register: nomad_client_remove_purge_posix
  changed_when: nomad_client_remove_purge_posix.rc == 0
  failed_when: false
  when:
    - nomad_client_remove_purge_node | bool
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - nomad_client_remove_self_node_status_posix.get('rc', 1) == 0
    - (nomad_client_remove_cli_path_posix.stdout | default('') | trim | length) > 0

- name: Purge node from Nomad server list (Windows)
  ansible.windows.win_shell: |
    $env:NOMAD_ADDR = "{{ nomad_client_remove_nomad_env_purge.NOMAD_ADDR | default('') }}"
    $env:NOMAD_TOKEN = "{{ nomad_client_remove_nomad_env_purge.NOMAD_TOKEN | default('') }}"
    $env:NOMAD_CACERT = "{{ nomad_client_remove_nomad_env_purge.NOMAD_CACERT | default('') }}"
    $env:NOMAD_CLIENT_CERT = "{{ nomad_client_remove_nomad_env_purge.NOMAD_CLIENT_CERT | default('') }}"
    $env:NOMAD_CLIENT_KEY = "{{ nomad_client_remove_nomad_env_purge.NOMAD_CLIENT_KEY | default('') }}"
    & "{{ nomad_client_remove_cli_path_windows.stdout | trim }}" node purge -yes "{{ nomad_client_remove_self_node_id_windows.stdout | trim }}"
  register: nomad_client_remove_purge_windows
  changed_when: nomad_client_remove_purge_windows.rc == 0
  failed_when: false
  when:
    - nomad_client_remove_purge_node | bool
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_self_node_id_windows.get('rc', 1) == 0
    - (nomad_client_remove_self_node_id_windows.stdout | default('') | trim | length) > 0
    - (nomad_client_remove_cli_path_windows.stdout | default('') | trim | length) > 0

- name: Recommend node purge when deleting client state
  ansible.builtin.debug:
    msg: >-
      Recommendation: nomad_client_remove_delete_state=true removes Nomad client identity
      state (node ID). Set nomad_client_remove_purge_node=true to remove stale node
      entries from the Nomad client list.
  when:
    - nomad_client_remove_delete_state | bool
    - not (nomad_client_remove_purge_node | bool)

# --- Package manager removal (RedHat / Debian) ---

- name: Remove Nomad package(s)
  ansible.builtin.package:
    name: "{{ nomad_client_remove_packages }}"
    state: absent
  when: nomad_client_remove_use_package_manager

- name: Remove HashiCorp yum repository
  ansible.builtin.yum_repository:
    name: hashicorp
    state: absent
  when:
    - nomad_client_remove_use_package_manager
    - ansible_facts["os_family"] == "RedHat"

- name: Remove HashiCorp apt repository
  ansible.builtin.apt_repository:
    repo: "deb https://apt.releases.hashicorp.com {{ ansible_facts['distribution_release'] }} main"
    state: absent
  when:
    - nomad_client_remove_use_package_manager
    - ansible_facts["os_family"] == "Debian"

- name: Remove HashiCorp GPG key
  ansible.builtin.apt_key:
    url: https://apt.releases.hashicorp.com/gpg
    state: absent
  when:
    - nomad_client_remove_use_package_manager
    - ansible_facts["os_family"] == "Debian"

# --- Direct download removal (Linux, non-package) ---

- name: Remove Nomad binary
  ansible.builtin.file:
    path: "{{ nomad_client_remove_install_dir }}/nomad"
    state: absent
  when:
    - ansible_facts["system"] in ["Linux", "Darwin"]
    - not nomad_client_remove_use_package_manager

- name: Remove Nomad systemd unit
  ansible.builtin.file:
    path: /etc/systemd/system/nomad.service
    state: absent
  when:
    - ansible_facts["system"] == "Linux"
    - not nomad_client_remove_use_package_manager

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  when:
    - ansible_facts["system"] == "Linux"
    - not nomad_client_remove_use_package_manager

# --- Direct download removal (macOS) ---

- name: Remove Nomad launchd plist
  ansible.builtin.file:
    path: "{{ nomad_client_remove_launchd_plist_path }}"
    state: absent
  when: ansible_facts["system"] == "Darwin"

# --- Direct download removal (Windows) ---

- name: Remove Nomad Windows service
  ansible.windows.win_service:
    name: "{{ nomad_client_remove_service_name }}"
    state: absent
  when: ansible_facts["os_family"] == "Windows"

- name: Force-stop residual Nomad processes (Windows)
  ansible.windows.win_shell: |
    $nomadProcesses = Get-Process -Name 'nomad' -ErrorAction SilentlyContinue
    if ($null -ne $nomadProcesses) {
      $nomadProcesses | Stop-Process -Force -ErrorAction SilentlyContinue
      Start-Sleep -Seconds 2
      Write-Output 'stopped'
    } else {
      Write-Output 'not-running'
    }
  register: nomad_client_remove_force_stop_windows
  failed_when: false
  changed_when: "'stopped' in (nomad_client_remove_force_stop_windows.stdout | default(''))"
  when: ansible_facts["os_family"] == "Windows"

- name: Remove Nomad binary (Windows)
  ansible.windows.win_file:
    path: "{{ nomad_client_remove_install_dir }}\\nomad.exe"
    state: absent
  register: nomad_client_remove_binary_windows
  retries: 6
  delay: 2
  until: nomad_client_remove_binary_windows is succeeded
  when: ansible_facts["os_family"] == "Windows"

# --- Common cleanup ---

- name: Remove Nomad configuration directory
  ansible.builtin.file:
    path: "{{ nomad_client_remove_config_dir }}"
    state: absent
  when: ansible_facts["os_family"] != "Windows"

- name: Remove Nomad state directory
  ansible.builtin.file:
    path: "{{ nomad_client_remove_state_dir }}"
    state: absent
  when:
    - ansible_facts["os_family"] != "Windows"
    - nomad_client_remove_delete_state | bool

- name: Unmount Nomad allocation mounts (Linux)
  ansible.builtin.shell: |
    set -euo pipefail
    if command -v findmnt >/dev/null 2>&1; then
      while IFS= read -r target; do
        umount -l "$target" || true
      done < <(findmnt -rn -o TARGET | awk '$0 ~ "^{{ (nomad_client_remove_data_dir ~ "/alloc") | regex_escape }}(/|$)"')
    fi
  args:
    executable: /bin/bash
  changed_when: false
  failed_when: false
  when:
    - ansible_facts["system"] == "Linux"
    - nomad_client_remove_delete_state | bool

- name: Remove Nomad allocation mounts directory
  ansible.builtin.file:
    path: "{{ nomad_client_remove_data_dir }}/alloc_mounts"
    state: absent
  when:
    - ansible_facts["os_family"] != "Windows"
    - nomad_client_remove_delete_state | bool

- name: Remove Nomad allocation directory
  ansible.builtin.file:
    path: "{{ nomad_client_remove_data_dir }}/alloc"
    state: absent
  when:
    - ansible_facts["os_family"] != "Windows"
    - nomad_client_remove_delete_state | bool

- name: Remove Nomad data directory
  ansible.builtin.file:
    path: "{{ nomad_client_remove_data_dir }}"
    state: absent
  when:
    - ansible_facts["os_family"] != "Windows"
    - nomad_client_remove_delete_state | bool

- name: Remove Nomad configuration directory (Windows)
  ansible.windows.win_file:
    path: "{{ nomad_client_remove_config_dir }}"
    state: absent
  when: ansible_facts["os_family"] == "Windows"

- name: Remove Nomad state directory (Windows)
  ansible.windows.win_file:
    path: "{{ nomad_client_remove_state_dir }}"
    state: absent
  when:
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_delete_state | bool

- name: Remove Nomad data directory (Windows)
  ansible.windows.win_file:
    path: "{{ nomad_client_remove_data_dir }}"
    state: absent
  when:
    - ansible_facts["os_family"] == "Windows"
    - nomad_client_remove_delete_state | bool
